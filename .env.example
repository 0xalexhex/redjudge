# Copy to .env (gitignored) and fill in. NEVER commit a real key.
# OpenRouter = ONE key for ALL models (openai/anthropic/google/xai/... via one gateway).
OPENROUTER_API_KEY=
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
